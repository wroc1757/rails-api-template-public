default: &default
  adapter: postgresql
  encoding: unicode
  max_connections: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  url: <%= ENV["DATABASE_URL"] %>

test:
  <<: *default
  database: rails_api_template_test

production:
  primary: &primary_production
    <<: *default
    url: <%= ENV["DATABASE_URL"] %>
  cache:
    <<: *primary_production
    database: rails_api_template_production_cache
    migrations_paths: db/cache_migrate
  queue:
    <<: *primary_production
    database: rails_api_template_production_queue
    migrations_paths: db/queue_migrate
  cable:
    <<: *primary_production
    database: rails_api_template_production_cable
    migrations_paths: db/cable_migrate
